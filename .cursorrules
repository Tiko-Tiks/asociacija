PROJECT: BENDRUOMENIŲ BRANDUOLYS
MODE: GOVERNANCE / AUDIT-SAFE
DB: Supabase (PostgreSQL)
SCHEMA STATUS: CODE FREEZE (v19.0)

You are working on an institutional governance system.
This is NOT a startup app or flexible schema project.

---

CORE RULES (ABSOLUTE)

1. DATABASE SCHEMA IS IMMUTABLE
- No new tables
- No new columns
- No ALTER TABLE
- No schema refactors

2. METADATA JSONB IS THE ONLY EXTENSION CHANNEL
- metadata always exists
- metadata is always an object
- metadata follows strict namespace rules

Allowed prefixes ONLY:
- fact.*
- indicator.*
- project.*
- ui.*
- template.*
- ai.*

Any other key is INVALID.

---

LEGAL ENFORCEMENT RULES

- APPROVED resolutions are IMMUTABLE
- You must NEVER suggest updating metadata of an APPROVED resolution
- Any change requires a NEW resolution with a reference

AI OUTPUT RULE:
- AI data is INTERPRETATION ONLY
- AI data has NO legal or procedural power
- AI data must be clearly labeled (ai.*)

---

IMPLEMENTATION RULES (SUPABASE)

- Validation logic goes into:
  - triggers
  - RPC functions
- UI must NOT update metadata directly
- Analytics go into SQL views
- Runtime calculations go into views or RPC

---

STRICTLY FORBIDDEN

- metadata.title
- metadata.status
- metadata.created_at
- duplicating structural columns inside metadata
- using metadata as a relational table

---

WHEN IN DOUBT

Default to:
- procedural safety
- audit clarity
- human confirmation

If a request conflicts with Governance principles:
→ You must REFUSE and explain why.

---

FINAL PRINCIPLE

The system does NOT create legitimacy.
It only records and protects legitimacy created by humans.